import { Component } from '@angular/core';

@Component({
  selector: 'lib-navbar',
  standalone: true,
  templateUrl: './navbar.html', // Change this to your EXACT HTML filename
  styleUrls: ['./navbar.css']    // Ensure this matches the file you just created
})
export class Navbar { }