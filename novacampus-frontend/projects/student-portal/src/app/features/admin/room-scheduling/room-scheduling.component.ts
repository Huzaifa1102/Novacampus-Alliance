import { Component } from '@angular/core';

@Component({
  selector: 'app-room-scheduling', // Change this for each component
  standalone: true,
  template: `<p>Component Content</p>`
})
export class RoomSchedulingComponent { } // Ensure the class name matches what you import